{
	"name": "most-promise-map",
	"version": "1.0.0",
	"description": "Map over a promise stream, splitting into resolved & rejected streams",
	"main": "most-promise-map.js",
	"dependencies": {
		"@most/create": "^2.0.1",
		"most": "^1.7.0"
	},
	"devDependencies": {},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "rektide de la faye",
	"license": "MIT"
}
